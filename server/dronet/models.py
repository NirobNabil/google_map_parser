from django.db import models

# Create your models here.

# class drone(models.Model):
#      = models.PositiveIntegerField("x", default=0)
#     y = models.PositiveIntegerField("y", default=0)
#     connected = models.CharField()
#     # playlist = models.ForeignKey(Playlist, on_delete=models.PROTECT, null=True)
#     def __str__(self):
#         return self.title